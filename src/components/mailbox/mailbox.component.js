import template from './mailbox.html';
import controller from './mailbox.controller';

let component = {
        restrict: 'E',
        bindings: {},
        template,
        controller
    };

export default component;

